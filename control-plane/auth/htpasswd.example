# Generate the live file with:
#   htpasswd -nbB operator 'the-shared-secret' > control-plane/auth/htpasswd
# Traefik reads bcrypt lines. Do not commit control-plane/auth/htpasswd.
operator:$2y$05$ReplaceThisHashWithARealBcryptFromHtpasswd
