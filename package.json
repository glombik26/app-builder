{
  "name": "app-builder",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "start": "node src/main.ts"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
